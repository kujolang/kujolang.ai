# Implemented changes

Audit date: 2026-08-14

- Added the Primitives, Tooling, and Showcase catalog URLs to generated `llms.txt` output.
- Removed redundant client-side re-dithering from already Kujo-dithered static WebP hero art.
- Scoped text scrambling to deliberate display elements instead of scanning every body text node.
- Aligned the mobile navigation accessible name with its visible `Menu`/`Close` label.
- Added deterministic contracts for every Publishing House route's GitHub link, Howl social card, sitemap entry, `llms.txt` entry, and absence of volatile `0.1.0` copy.
